CREATE TABLE courses(
	course_name varchar(255),
	semester varchar(255),
	professor_name varchar(255)
);


-- ALTER TABLE courses
-- ADD CONSTRAINT PRIMARY KEY(id); -- note that with Postgres, you do not need the word CONSTRAINT